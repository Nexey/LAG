<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5g">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        {% block navbar %}
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                {% if app.user %}
                    <li lass="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
                {% else %}
                    <li lass="nav-item"><a class="nav-link" href="/login">Login</a></li>
                    <li lass="nav-item"><a class="nav-link" class="nav-link" href="/register">Register</a></li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li lass="nav-item"><a class="nav-link" href="/admin">Dashboard</a></li>
                {% endif %}
            </ul>
        {% endblock %}
    </div>
</nav>